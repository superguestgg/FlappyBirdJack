class Main
{
    function void main()
    { 
        var FlappyBird game;
var char gameStarted;
        let game = FlappyBird.new();
	  do game.prestart();
        
        // let gameStarted = Keyboard.readChar();
        // Здесь начинается игра

        do game.run();

        //do game.dispose();
        return;
    }
}